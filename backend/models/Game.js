const mongoose = require('mongoose');const gameSchema = new mongoose.Schema({board: {type: Array,default: Array(9).fill(null),},currentPlayer: {type: String,default: 'X',},winner: {type: String,default: null,},}, {timestamps: true});module.exports = mongoose.model('Game', gameSchema);